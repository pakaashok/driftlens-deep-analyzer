apiVersion: v1
kind: Service
metadata:
  name: driftlens-deep-backend
  labels:
    app: driftlens-deep
    component: backend
spec:
  selector:
    app: driftlens-deep
    component: backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
